---
phase: 06-cli-install
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CLI-08]
must_haves:
  truths:
    - "A fresh-VM install test job runs on ubuntu-latest, macos-latest, and windows-latest runners as part of CI"
    - "The job starts from a clean GitHub Actions VM (no cached ~/.cyrus/, no existing ~/.claude/settings.json) and runs `pip install -e . && cyrus init && cyrus doctor --json`"
    - "The job exits non-zero if any step fails, gating the HARD RELEASE GATE in ROADMAP Phase 6 success criterion #4"
    - "The job complements the existing `test` job — it does not replace unit tests; it validates the install path end-to-end"
    - "Doctor's MCP-canary and settings-hook checks pass in the CI environment after `cyrus init` runs (proving end-to-end wiring)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "New `install-test` job with 3-OS matrix running `pip install -e . && cyrus init && cyrus doctor --json`"
      contains: "install-test"
  key_links:
    - from: ".github/workflows/ci.yml [install-test job]"
      to: "src/cyrus/_init.py::run (via console script `cyrus init`)"
      via: "console script defined in pyproject.toml [project.scripts] cyrus = cyrus.cli:main"
      pattern: "cyrus init"
    - from: ".github/workflows/ci.yml [install-test job]"
      to: "src/cyrus/_doctor.py::run (via `cyrus doctor --json`)"
      via: "parses JSON output and asserts `all_ok == true` for required checks"
      pattern: "cyrus doctor"
---

<objective>
Add the fresh-VM install-test CI job that closes CLI-08 — the HARD RELEASE GATE from ROADMAP Phase 6. On every push and PR, an ephemeral GitHub Actions runner on each of the three supported OSes must successfully execute `pip install -e . && cyrus init && cyrus doctor --json` with exit 0, proving the install path that MemPalace failed at.

Purpose: This is the phase's gate. Without it, the release criterion "a vanilla VM can run the install sequence with no manual fixups" is unverified. Catching install regressions in CI is cheaper than catching them in the release checklist.

Output: A new `install-test` job added to `.github/workflows/ci.yml`, matrix on `[ubuntu-latest, macos-latest, windows-latest]` × Python 3.11 only (one version is sufficient for install-smoke; matrix of 3×3 is the `test` job's job). Covers CLI-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cli-install/06-CONTEXT.md
@.planning/phases/06-cli-install/06-01-PLAN.md

@.github/workflows/ci.yml
@CONTRIBUTING.md

<interfaces>
<!-- `cyrus init` and `cyrus doctor --json` are delivered by Plan 06-01. This plan -->
<!-- consumes them as black-box CLI entry points. Contract: -->
<!--   - `cyrus init` exits 0 on fresh install, stdout contains "claude mcp add" hint -->
<!--   - `cyrus doctor --json` emits JSON with `{"checks":[...], "all_ok": bool}` -->
<!--   - Doctor's `mcp_canary` and `cyrus_on_path` checks must pass after `pip install -e .` -->
<!--   - Doctor's `settings_hook_registered` check must pass after `cyrus init` -->

<!-- The `cyrus_on_path` doctor check depends on `pip install -e .` creating the -->
<!-- `cyrus` console script on PATH. pyproject.toml already declares this via -->
<!-- [project.scripts] cyrus = "cyrus.cli:main". -->

<!-- Existing CI workflow structure (.github/workflows/ci.yml): -->
<!--   jobs.test — matrix 3 OS × 3 Python versions, runs unit tests -->
<!-- New job `install-test` is INDEPENDENT (no `needs:`) so it runs in parallel. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install-test CI job to ci.yml</name>
  <read_first>
    - .github/workflows/ci.yml (existing `test` job structure)
    - .planning/phases/06-cli-install/06-CONTEXT.md (fresh-VM install test decisions block)
    - .planning/phases/06-cli-install/06-01-PLAN.md (contracts of `cyrus init` and `cyrus doctor --json`)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    1. Read existing `.github/workflows/ci.yml`. Preserve the existing `test` job verbatim.

    2. Append a new `install-test` job under `jobs:` (sibling to `test`). Full YAML to add:

       ```yaml
         install-test:
           name: Fresh-VM install (${{ matrix.os }})
           runs-on: ${{ matrix.os }}
           strategy:
             fail-fast: false
             matrix:
               os: [ubuntu-latest, macos-latest, windows-latest]
           steps:
             - uses: actions/checkout@v4

             - name: Set up Python 3.11
               uses: actions/setup-python@v5
               with:
                 python-version: "3.11"

             - name: Install package (simulates `pip install cyrus` on fresh VM)
               run: pip install -e .

             - name: Verify cyrus console script is on PATH
               shell: bash
               run: |
                 which cyrus || command -v cyrus
                 cyrus --help

             - name: Run cyrus init
               shell: bash
               run: |
                 cyrus init
                 echo "--- ~/.cyrus/ tree ---"
                 ls -la "$HOME/.cyrus" || dir "%USERPROFILE%\\.cyrus"
                 echo "--- ~/.claude/settings.json ---"
                 cat "$HOME/.claude/settings.json" || type "%USERPROFILE%\\.claude\\settings.json"

             - name: Run cyrus doctor (JSON mode) and assert critical checks pass
               shell: bash
               run: |
                 set -euo pipefail
                 cyrus doctor --json > doctor.json
                 cat doctor.json
                 python - <<'PY'
                 import json, sys
                 data = json.loads(open("doctor.json").read())
                 checks = {c["name"]: c for c in data["checks"]}
                 required = [
                     "python_version",
                     "cyrus_on_path",
                     "cyrus_home_writable",
                     "settings_hook_registered",
                     "mcp_canary",
                     "kill_switch",
                 ]
                 failures = [n for n in required if not checks.get(n, {}).get("ok")]
                 if failures:
                     print(f"FAIL: required checks not OK: {failures}", file=sys.stderr)
                     for n in failures:
                         print(f"  {n}: {checks.get(n, {}).get('detail', '<missing>')}", file=sys.stderr)
                     sys.exit(1)
                 print("All required doctor checks passed.")
                 PY

             - name: Run cyrus init again (idempotency smoke)
               shell: bash
               run: |
                 set -euo pipefail
                 cyrus init
                 python - <<'PY'
                 import json, os
                 p = os.path.expanduser("~/.claude/settings.json")
                 data = json.loads(open(p).read())
                 count = 0
                 for entry in (data.get("hooks") or {}).get("PreToolUse") or []:
                     for h in entry.get("hooks") or []:
                         if h.get("command") == "cyrus hook run":
                             count += 1
                 assert count == 1, f"expected exactly 1 cyrus hook entry, got {count}"
                 print("Idempotency OK: exactly one cyrus hook entry.")
                 PY
       ```

       Key design decisions:
       - `shell: bash` on every step — Windows runners ship with Git Bash, so this works uniformly across the 3 OSes and avoids PowerShell/cmd escaping hell.
       - Python 3.11 only in this job (not 3×3) — the `test` job already covers the full version matrix. The install-smoke only needs one Python version per OS to prove the CLI-08 release gate.
       - `set -euo pipefail` in multi-line shell blocks so silent failures are caught.
       - The assertion script is embedded via heredoc (`python - <<'PY' ... PY`) — no extra scratch files, pure stdlib, mirrors the rest of Cyrus's stdlib-only discipline.
       - `fail-fast: false` mirrors the existing `test` job — failures on one OS shouldn't mask the others.
       - No `needs:` clause — the install-test is independent of the unit-test job and they should run in parallel.
       - The fallback `|| dir ...` and `|| type ...` branches in the `ls` / `cat` steps are defensive: if bash resolution of `$HOME` ever fails on Windows, we still get a human-readable listing in the log.

    3. Commit:
       ```bash
       git add .github/workflows/ci.yml
       git commit -m "ci(06-02): add fresh-VM install-test job for CLI-08 release gate"
       ```

    4. (Optional, cannot be verified locally — documented for the executor's awareness): Push and confirm the job runs green on all 3 OSes. If any OS fails, triage:
       - Windows doctor failure on `cyrus_on_path`: pip may install the script into a Scripts dir not on PATH; the `pip install -e .` step should handle this via setup-python@v5's default PATH wiring.
       - macOS MCP-canary timeout: the canary timeout is 5s in _doctor.py; bump if needed in a follow-up plan.
  </action>
  <verify>
    <automated>python -c "import yaml, sys; y = yaml.safe_load(open('.github/workflows/ci.yml')); assert 'install-test' in y['jobs'], 'install-test job missing'; j = y['jobs']['install-test']; assert j['strategy']['matrix']['os'] == ['ubuntu-latest', 'macos-latest', 'windows-latest'], f'unexpected matrix: {j[\"strategy\"][\"matrix\"]}'; steps = [s.get('name','') for s in j['steps']]; assert any('cyrus init' in s for s in steps), 'no cyrus init step'; assert any('doctor' in s.lower() for s in steps), 'no doctor step'; print('OK: install-test job valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a job named `install-test` at the top level of `jobs:`.
    - The job's matrix lists all three of `ubuntu-latest`, `macos-latest`, `windows-latest`.
    - The job runs `pip install -e .`, `cyrus init`, and `cyrus doctor --json` in that order.
    - The doctor JSON is parsed and the job fails non-zero if any of the 6 required checks (`python_version`, `cyrus_on_path`, `cyrus_home_writable`, `settings_hook_registered`, `mcp_canary`, `kill_switch`) return `ok: false`.
    - An idempotency step re-runs `cyrus init` and asserts exactly one `cyrus hook run` entry in settings.json.
    - The existing `test` job is unchanged (diff only adds the new job).
    - `grep -c "install-test" .github/workflows/ci.yml` returns 1 or more.
  </acceptance_criteria>
  <done>CLI-08 release gate wired into CI; Phase 6 HARD RELEASE GATE from ROADMAP Phase 6 success criterion #4 is now continuously verified on every push.</done>
</task>

</tasks>

<verification>
Requirement coverage:
- CLI-08: Fresh-VM install test on ubuntu/macos/windows × Python 3.11 runs `pip install -e . && cyrus init && cyrus doctor`. Covered by Task 1.

Note: The ROADMAP specifies the fresh-VM install uses `pip install <name>` (i.e., from PyPI). Because v0.0.0 has not yet been published to PyPI (deferred from Plan 00-02 per 06-CONTEXT.md), we substitute `pip install -e .` — functionally equivalent for the install-smoke goal (simulates pip installing the package into a clean environment) and this is exactly what the CONTEXT.md specifies. When Phase 7 ships v0.1.0 to PyPI, a follow-up plan can switch to `pip install cyrus==<version>` in a tag-gated second job.

Not covered by this plan (out of scope for CLI-08, covered elsewhere):
- `claude mcp add cyrus -- cyrus serve` is NOT run in CI because the GitHub runner has no Claude Code installed. The MCP canary inside `cyrus doctor` is the proxy for that check — it spawns `cyrus serve` as a subprocess and performs the JSON-RPC initialize handshake. That proves the server works end-to-end without requiring Claude Code in CI.
</verification>

<success_criteria>
- `.github/workflows/ci.yml` contains the new `install-test` job with the 3-OS matrix.
- The job runs `pip install -e . && cyrus init && cyrus doctor --json` and fails if required doctor checks fail.
- The job re-runs `cyrus init` and asserts idempotency (exactly 1 cyrus hook entry).
- The existing `test` job in ci.yml is unchanged.
- When pushed, the job passes green on all 3 OSes. (Verified manually by observing the PR check — this is outside the plan's automated verify, which only validates YAML structure.)
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-install/06-02-SUMMARY.md` per the summary template.
</output>
