---
phase: 00-setup-naming-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/cyrus/__init__.py
  - src/cyrus/py.typed
  - LICENSE
  - README.md
  - CONTRIBUTING.md
  - .github/workflows/ci.yml
  - .gitignore
  - tests/__init__.py
  - tests/test_placeholder.py
autonomous: true
requirements: [SETUP-02, SETUP-03, SETUP-04]

must_haves:
  truths:
    - "pyproject.toml declares Python >=3.11, uses hatchling, has zero runtime dependencies"
    - "GitHub repo github.com/Mo-Hendawy/cyrus exists with MIT LICENSE, README skeleton, and CONTRIBUTING.md on main"
    - "CI matrix runs on Windows + macOS + Linux x Python 3.11, 3.12, 3.13 and passes on a placeholder test"
    - "src/cyrus/ package layout exists with __init__.py exporting __version__"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata and build configuration"
      contains: "requires-python = \">=3.11\""
    - path: "src/cyrus/__init__.py"
      provides: "Package root with version"
      contains: "__version__"
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: "README.md"
      provides: "Project skeleton README"
      contains: "cyrus"
    - path: "CONTRIBUTING.md"
      provides: "Contribution guidelines"
      contains: "Contributing"
    - path: ".github/workflows/ci.yml"
      provides: "CI matrix configuration"
      contains: "python-version"
    - path: "tests/test_placeholder.py"
      provides: "No-op test for CI validation"
      contains: "test_placeholder"
  key_links:
    - from: "pyproject.toml"
      to: "src/cyrus/__init__.py"
      via: "hatchling src layout packages config"
      pattern: "packages.*src/cyrus"
    - from: ".github/workflows/ci.yml"
      to: "tests/test_placeholder.py"
      via: "python -m unittest discover"
      pattern: "unittest"
---

<objective>
Create the complete repository scaffolding for the Cyrus project: pyproject.toml with hatchling build backend and Python 3.11+ minimum, src/ layout package structure, MIT LICENSE, README skeleton, CONTRIBUTING.md, .gitignore, a placeholder test, and a GitHub Actions CI matrix for Windows + macOS + Linux x Python 3.11/3.12/3.13.

Purpose: Stand up the empty-but-correct repository so Phase 1 can start writing code without making structural decisions mid-flight. This addresses SETUP-02 (pyproject.toml), SETUP-03 (CI matrix), and SETUP-04 (GitHub repo with docs).

Output: A complete, buildable Python package scaffolding with passing CI on all 9 matrix cells (3 OSes x 3 Python versions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package scaffolding and project files</name>
  <files>
    pyproject.toml
    src/cyrus/__init__.py
    src/cyrus/py.typed
    LICENSE
    README.md
    CONTRIBUTING.md
    .gitignore
    tests/__init__.py
    tests/test_placeholder.py
  </files>
  <read_first>
    .planning/PROJECT.md
    .planning/research/STACK.md
    .planning/ROADMAP.md
  </read_first>
  <action>
Create the src/ layout package structure and all scaffolding files. Remove the old empty `cortex/` directory if it exists.

**pyproject.toml** — Create with these EXACT contents:

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cyrus"
version = "0.0.0"
description = "Zero-dependency AI memory system with hook-level rules enforcement for Claude Code"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Mo Hendawy", email = "moh.abdelmaksoad@gmail.com" },
]
keywords = ["ai", "memory", "claude", "mcp", "rules"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
]

[project.urls]
Homepage = "https://github.com/Mo-Hendawy/cyrus"
Repository = "https://github.com/Mo-Hendawy/cyrus"
Issues = "https://github.com/Mo-Hendawy/cyrus/issues"

[project.scripts]
cyrus = "cyrus.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/cyrus"]
```

CRITICAL constraints per locked decisions:
- `name = "cyrus"` (per SETUP-01 — name confirmed available on PyPI 2026-04-11)
- `requires-python = ">=3.11"` (per locked decision — 3.9 is EOL, 3.11 gives tomllib + faster cold-start)
- `build-backend = "hatchling.build"` (per locked decision)
- NO `[project.dependencies]` key at all — zero runtime deps (per SETUP-02)
- `version = "0.0.0"` — placeholder version for initial PyPI reservation

**src/cyrus/__init__.py** — Create with:
```python
"""Cyrus - Zero-dependency AI memory system with hook-level rules enforcement."""

__version__ = "0.0.0"
```

**src/cyrus/py.typed** — Create as empty file (PEP 561 marker for type checkers).

**LICENSE** — Create MIT License file with:
- Copyright year: 2026
- Copyright holder: Mo Hendawy
- Full MIT license text

**README.md** — Create a skeleton README:
```markdown
# Cyrus

Zero-dependency AI memory system with hook-level rules enforcement for Claude Code.

> **Status:** Pre-alpha. Not yet published.

## What is Cyrus?

Cyrus gives your AI persistent memory across sessions -- conversations, decisions, preferences -- stored as plain markdown files. The differentiator: rules enforcement at the hook level that survives `--dangerously-skip-permissions`.

## Installation

Coming soon.

## License

MIT
```

**CONTRIBUTING.md** — Create with:
```markdown
# Contributing to Cyrus

## Development Setup

1. Clone the repository:
   ```
   git clone https://github.com/Mo-Hendawy/cyrus.git
   cd cyrus
   ```

2. Create a virtual environment:
   ```
   python -m venv .venv
   source .venv/bin/activate  # or .venv\Scripts\activate on Windows
   ```

3. Install in development mode:
   ```
   pip install -e .
   ```

4. Run tests:
   ```
   python -m unittest discover -s tests -v
   ```

## Running Tests

```
python -m unittest discover -s tests -v
```

Tests must pass on Windows, macOS, and Linux with Python 3.11, 3.12, and 3.13.

## Pull Requests

- All PRs must pass CI on all matrix cells before merge
- Zero runtime dependencies -- do not add entries to `[project.dependencies]`
- Use `pathlib.Path` for all filesystem operations -- `os.path` is banned
- All logging goes to stderr -- stdout is reserved for protocol messages

## License

By contributing, you agree that your contributions will be licensed under the MIT License.
```

**.gitignore** — Create a Python .gitignore including:
```
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/
.env
*.so
.Python
develop-eggs/
pip-log.txt
pip-delete-this-directory.txt
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
```

**tests/__init__.py** — Create as empty file.

**tests/test_placeholder.py** — Create with:
```python
"""Placeholder test to validate CI pipeline."""

import unittest


class TestPlaceholder(unittest.TestCase):
    """Placeholder test suite -- replaced in Phase 1."""

    def test_placeholder(self):
        """Verify test infrastructure works."""
        self.assertTrue(True)

    def test_import_cyrus(self):
        """Verify the cyrus package is importable."""
        import cyrus
        self.assertEqual(cyrus.__version__, "0.0.0")


if __name__ == "__main__":
    unittest.main()
```
  </action>
  <verify>
    <automated>cd /c/Users/mohab/gsd-workspaces/cortex &amp;&amp; python -c "import tomllib; t=open('pyproject.toml','rb'); d=tomllib.load(t); assert d['project']['name']=='cyrus'; assert d['project']['requires-python']=='>=3.11'; assert 'dependencies' not in d['project']; assert d['build-system']['build-backend']=='hatchling.build'; print('pyproject.toml OK')" &amp;&amp; pip install -e . &amp;&amp; python -m unittest discover -s tests -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists and contains `name = "cyrus"`
    - pyproject.toml contains `requires-python = ">=3.11"`
    - pyproject.toml contains `build-backend = "hatchling.build"`
    - pyproject.toml does NOT contain a `[project.dependencies]` key
    - pyproject.toml contains `version = "0.0.0"`
    - src/cyrus/__init__.py exists and contains `__version__ = "0.0.0"`
    - src/cyrus/py.typed exists
    - LICENSE exists and contains "MIT License"
    - README.md exists and contains "Cyrus"
    - CONTRIBUTING.md exists and contains "Contributing to Cyrus"
    - .gitignore exists and contains "__pycache__/"
    - tests/test_placeholder.py exists and contains "test_placeholder"
    - tests/test_placeholder.py contains "test_import_cyrus"
    - `pip install -e .` succeeds
    - `python -m unittest discover -s tests -v` passes with 2 tests OK
    - The old empty `cortex/` directory does not exist
  </acceptance_criteria>
  <done>All scaffolding files created. Package installs in editable mode. Two placeholder tests pass (basic assertion + cyrus import with version check). No runtime dependencies declared.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub repo, CI workflow, and push to main</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    pyproject.toml
    tests/test_placeholder.py
    .planning/research/STACK.md
  </read_first>
  <action>
**Step 1: Create .github/workflows/ci.yml** with this EXACT content:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: pip install -e .

      - name: Run tests
        run: python -m unittest discover -s tests -v
```

Key points:
- `fail-fast: false` so all 9 matrix cells run even if one fails
- 3 OSes: ubuntu-latest, macos-latest, windows-latest (per SETUP-03)
- 3 Python versions: 3.11, 3.12, 3.13 (per SETUP-03)
- Installs package in editable mode before running tests (validates pyproject.toml works)
- Runs `python -m unittest discover -s tests -v` (stdlib only, no pytest)

**Step 2: Create GitHub repo and push**

Run these commands in sequence:
```bash
# Create the GitHub repo (public, MIT license already in files)
gh repo create Mo-Hendawy/cyrus --public --description "Zero-dependency AI memory system with hook-level rules enforcement for Claude Code" --source . --push

# If the repo already exists, just add the remote and push:
# git remote add origin https://github.com/Mo-Hendawy/cyrus.git
# git push -u origin main
```

If `gh repo create` fails because the repo already exists, fall back to adding the remote manually and pushing.

**Step 3: Verify CI runs**

After pushing to main, check that CI was triggered:
```bash
gh run list --limit 1
```

Wait for CI to complete (it should take 2-3 minutes), then verify all 9 matrix cells passed:
```bash
gh run view --log  # or gh run watch
```

If any matrix cell fails, diagnose and fix before considering this task done.
  </action>
  <verify>
    <automated>cd /c/Users/mohab/gsd-workspaces/cortex &amp;&amp; gh repo view Mo-Hendawy/cyrus --json name,url 2>/dev/null &amp;&amp; test -f .github/workflows/ci.yml &amp;&amp; grep -q "python-version.*3.11.*3.12.*3.13" .github/workflows/ci.yml &amp;&amp; grep -q "ubuntu-latest.*macos-latest.*windows-latest" .github/workflows/ci.yml &amp;&amp; echo "REPO AND CI OK"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists
    - CI workflow contains matrix with os: [ubuntu-latest, macos-latest, windows-latest]
    - CI workflow contains matrix with python-version: ["3.11", "3.12", "3.13"]
    - CI workflow contains `fail-fast: false`
    - CI workflow runs `python -m unittest discover -s tests -v`
    - GitHub repo Mo-Hendawy/cyrus exists and is public
    - `gh repo view Mo-Hendawy/cyrus` succeeds
    - All files (pyproject.toml, LICENSE, README.md, CONTRIBUTING.md, .gitignore, src/, tests/, .github/) are pushed to main
    - CI workflow has been triggered and all 9 matrix cells pass (or are in progress)
  </acceptance_criteria>
  <done>GitHub repo Mo-Hendawy/cyrus is public, all scaffolding committed to main, CI matrix runs on 3 OSes x 3 Python versions, placeholder tests pass on all 9 cells.</done>
</task>

</tasks>

<verification>
1. `gh repo view Mo-Hendawy/cyrus --json name,url` returns the repo URL
2. `pip install -e .` in a clean venv succeeds with zero dependencies installed
3. `python -m unittest discover -s tests -v` passes locally
4. `gh run list --limit 1` shows a CI run that passed (all 9 cells green)
5. `grep -c 'dependencies' pyproject.toml` returns 0 (no runtime deps key)
6. `python -c "import cyrus; print(cyrus.__version__)"` prints "0.0.0"
</verification>

<success_criteria>
- GitHub repo exists at github.com/Mo-Hendawy/cyrus with MIT LICENSE, README, CONTRIBUTING.md
- pyproject.toml uses hatchling, Python >=3.11, zero runtime deps
- CI matrix (3 OS x 3 Python) runs and passes on placeholder test
- Package is installable and importable
</success_criteria>

<output>
After completion, create `.planning/phases/00-setup-naming-gate/00-01-SUMMARY.md`
</output>
